# Python cache
__pycache__/
*.pyc

# Virtual environment
.venv/
venv/

# Datasets (too big for git)
Data/
data/
*.csv

# Models: ignore everything except the deploy agent (seed 2) + its VecNormalize
# stats, which the live Streamlit dashboard needs at runtime.
models/*
!models/ppo_wind_turbine_seed2.zip
!models/vecnormalize_seed2.pkl
logs/

# Generated results (kept small; regenerate with src/evaluate.py)
results/*.csv

# IDE / tooling
.vscode/
.qodo/
.mypy_cache/
